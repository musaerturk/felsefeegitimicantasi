import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ogretmenuygulama.com',
  appName: 'felsefe eğitimi çantası',
  webDir: 'dist'
};

export default config;
